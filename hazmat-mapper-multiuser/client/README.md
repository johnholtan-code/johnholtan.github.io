# Client App

Planned client modules:

- `src/auth/` — login/session handling.
- `src/map/` — map view + hazard overlays.
- `src/ui/` — reusable UI components.
- `src/api/` — API and websocket adapters.
- `src/utils/` — shared client-side utilities.
